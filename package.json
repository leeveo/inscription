{
  "name": "nexjs-inscriptions",
  "version": "1.0.0",
  "scripts": {
    "build": "cd event-admin && npm install && npm run build",
    "start": "cd event-admin && npm start",
    "dev": "cd event-admin && npm run dev",
    "install": "cd event-admin && npm install"
  },
  "dependencies": {
    "next": "14.2.24",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
